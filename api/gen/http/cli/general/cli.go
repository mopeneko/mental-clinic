// Code generated by goa v3.14.6, DO NOT EDIT.
//
// general HTTP client CLI support package
//
// Command:
// $ goa gen github.com/mopeneko/mental-clinic/api/design -o api

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	generalc "github.com/mopeneko/mental-clinic/api/gen/http/general/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() string {
	return `general (health-check|auth)
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` general health-check` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		generalFlags = flag.NewFlagSet("general", flag.ContinueOnError)

		generalHealthCheckFlags = flag.NewFlagSet("health-check", flag.ExitOnError)

		generalAuthFlags = flag.NewFlagSet("auth", flag.ExitOnError)
	)
	generalFlags.Usage = generalUsage
	generalHealthCheckFlags.Usage = generalHealthCheckUsage
	generalAuthFlags.Usage = generalAuthUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "general":
			svcf = generalFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "general":
			switch epn {
			case "health-check":
				epf = generalHealthCheckFlags

			case "auth":
				epf = generalAuthFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "general":
			c := generalc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "health-check":
				endpoint = c.HealthCheck()
				data = nil
			case "auth":
				endpoint = c.Auth()
				data = nil
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// generalUsage displays the usage of the general command and its subcommands.
func generalUsage() {
	fmt.Fprintf(os.Stderr, `Service for general users
Usage:
    %[1]s [globalflags] general COMMAND [flags]

COMMAND:
    health-check: HealthCheck implements healthCheck.
    auth: Auth implements auth.

Additional help:
    %[1]s general COMMAND --help
`, os.Args[0])
}
func generalHealthCheckUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] general health-check

HealthCheck implements healthCheck.

Example:
    %[1]s general health-check
`, os.Args[0])
}

func generalAuthUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] general auth

Auth implements auth.

Example:
    %[1]s general auth
`, os.Args[0])
}
